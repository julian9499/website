<html>
<head>
    <style>
        @keyframes font {
            0%   {font-size: 3.6vw;}
            50%  {font-size: 4.1vw;}
            100% {font-size: 3.6vw;}
        }

        @keyframes rotate { 
            0% { transform: rotate(0deg); }
            25% { transform: rotate(15deg); }
            50% { transform: rotate(0deg); }
            75% { transform: rotate(-15deg); }
            100% { transform: rotate(0deg); }
        }

        .wrapper { 
            height: 100%;
            width: 100%;
            left:0;
            right: 0;
            top: 0;
            bottom: 0;
            position: absolute;
            background: linear-gradient(124deg, #ff2400, #e81d1d, #e8b71d, #e3e81d, #1de840, #1ddde8, #2b1de8, #dd00f3, #dd00f3);
            background-size: 1800% 1800%;
            animation: rainbow 18s ease infinite;
        }

        @keyframes rainbow { 
            0%{background-position:0% 82%}
            50%{background-position:100% 19%}
            100%{background-position:0% 82%}
        }

        .text {
            animation: font 0.545454545454s 2.5s infinite, rotate 4.363636363636s 2.5s infinite;
        }

        @font-face {
            font-family: "Comic Sans WF";
            src: url("assets/comic_sans.ttf") format("truetype");
        }

        body {
            overflow-x: hidden;
        }

        .content {
            width: 100%;
            height: 10%;

            position:absolute;
            left:0; right:0;
            top:0; bottom:0;
            margin:auto;

            max-width:100%;
            max-height:100%;
            font-size: 3.6vw;
            text-align: center;
            overflow: visible;
            font-family: "Comic Sans WF", "Comic Sans MS", "Comic Sans";
        }
    </style>
    <script>
        setInterval(function() {
        var ajax = new XMLHttpRequest();
        ajax.onreadystatechange = function()
        {
            if(ajax.readyState == 4 && ajax.status == 200)
            {
                document.getElementById('dropouts').textContent = parseFloat(ajax.responseText).toFixed(2);
            }
        }
        ajax.open("get", "/api/dropouts"); 
        ajax.send();
    }, 1000);
    </script>
</head>
<body>
    <div class="wrapper">
        <div style="display:none;overflow:hidden">
            <iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/rY0WxgSXdEE?rel=0&amp;controls=0&amp;showinfo=0&amp;start=5&amp;autoplay=1&amp;loop=1&amp;playlist=rY0WxgSXdEE" frameborder="0" allowfullscreen></iframe>
        </div>
        <div class='content'>
            <div class='text'>
                Minstens <span id="dropouts">${data.dropouts}</span> mensen hebben CSE al opgegeven
            </div>
        </div>
    </div>
</body>
</html>